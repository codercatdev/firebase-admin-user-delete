# Remove All Users from Auth

[Admin SDK](https://console.firebase.google.com/project/dev-codingcat-dev/settings/serviceaccounts/adminsdk)

Download new private Key and rename to `serviceAccountKey.json`

Run below:

```bash
node index.js
```